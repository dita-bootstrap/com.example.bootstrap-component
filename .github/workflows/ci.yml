name: CI
'on':
  push:
    branches:
      - master
      - develop
  workflow_dispatch:

jobs:
  build-dita:
    name: Build DITA
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v6

      - name: Build Bootstrap Component
        uses: dita-ot/dita-ot-action@4.4
        with:
          plugins: |
            https://github.com/dita-bootstrap/dita-bootstrap.specialization/archive/master.zip
            https://github.com/infotexture/dita-bootstrap/archive/develop.zip
          project: .github/dita-ot/html.xml
